# Folge 01 V2 (Drehbuch) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das abgenommene Drehbuch V2 (`docs/story/DREHBUCH_FOLGE_01_V2.md` — BINDENDE Textquelle für alle deutschen Kästen/Gedanken und japanischen Dialoge) wird die laufende Folge 01: 10 dichte Panels, drehbuch-eigene Aufgabentexte, Zettel-Trace in P2, ~30 Wort-Vorkommen mit automatisiertem Dichte-Test, Prototyp-Panels aus den vorhandenen Renders.

**Architecture:** Drei Schichten: (1) kleine Schema-/Reader-Erweiterung — Interaktionen tragen optional `promptText` (Aufgabe aus dem Drehbuch), `target` (explizites Sprech-/Zeichenziel statt Bubble-Ableitung) und `targetItemIds` (explizite SRS-Buchung, `[]` = keine); (2) die Fixture wird nach dem Drehbuch neu gebaut (Panel-Bauplan unten ist die vollständige Bauanleitung); (3) das Lettering-Skript mappt alte Renders auf die 10 neuen Panels. Alles auf Branch `feat/reader-erleben` (baut auf dem fertigen Reader-Erleben-Stand auf).

**Tech Stack:** Flutter/Dart, Python 3 + Pillow (Font `/usr/share/fonts/truetype/droid/DroidSansFallbackFull.ttf`).

## Global Constraints

- **Drehbuch ist Gesetz:** Alle deutschen Erzählkästen/Gedanken und japanischen Dialoge WÖRTLICH aus `docs/story/DREHBUCH_FOLGE_01_V2.md` übernehmen (der Bauplan unten nennt pro Panel, WELCHE Texte wohin gehören; bei Wortlaut-Zweifel gewinnt das Drehbuch).
- Budget unverändert: dieselben 8 Lexeme (`lex_ja_sumimasen, lex_ja_ame, lex_ja_kasa, lex_ja_kore, lex_ja_kowareta, lex_ja_hai, lex_ja_douzo, lex_ja_arigatou`), Glyphen `あ/め/か` — Episode-`id`/`title`/`locale` bleiben (`ep_ja_shotengai_01`, „Regen", ja).
- **Dichte-Soll (testbar):** Bubble-Token gesamt ≥ 28 UND (Bubble-Token + Interaktions-`target`-Treffer) ≥ 30 UND jedes der 8 Budget-Wörter ≥ 2 Vorkommen (Bubbles + targets zusammen).
- Deutsch = Bedienung/Erzählstimme, Japanisch = Welt. Kein japanischer Meta-Text.
- Neue Schema-Felder optional mit Default = bisheriges Verhalten (kein bestehender Test bricht durch Task 1 allein).
- „Grün" = Full-Suite-Fehler sind ausschließlich die 8 vorbestehenden in `test/mining_packs/ja/`.
- Keine neuen Dart-Dependencies; Python nur PIL.
- Kommandos aus dem Worktree-Root (`/home/uli/projects/nihongo/.claude/worktrees/reader-erleben`).

---

## Der Panel-Bauplan (vollständige Bauanleitung für Task 2 + 3)

**Slots** (normierte Rechtecke x,y,w,h; hitArea = 4-Punkt-Polygon, x2=x+w, y2=y+h):
S1=(0.52,0.05,0.42,0.13) oben rechts · S2=(0.06,0.05,0.42,0.13) oben links · S2b=(0.06,0.20,0.36,0.11) links darunter · M=(0.30,0.36,0.40,0.13) mittig.

| Panel | Quell-Render (P##.png) | Bubbles: Sprecher → Text → Tokens[itemId] → Slot | thoughts (aus Drehbuch: Kästen, dann Gedanke in „ ") | Interaktion |
|---|---|---|---|---|
| 1 | P01 | signage → みなみまち → [みなみまち∅] → M | K1+K2+K3 („Das ist Mira…", „Ihre Großmutter…", „Sie kann nichts lesen…") + „Ich hätte anrufen sollen. Aber wen?" | – |
| 2 | P02 | – (keine) | „Der Regen war schneller als sie." + „Nein — nein, nicht jetzt. Nicht auch das noch." | trace, promptText=„Rette das Zeichen: Zeichne め nach, bevor der Regen es holt.", target=め, targetItemIds=[], reactionAsset=assets/story/p02_reaction.jpg, reactionCaption=„め. Was immer es heißt — jetzt gehört es ihr." |
| 3 | P04 | passant_a → あめ！ → [あめ=lex_ja_ame] → S2; passant_b → あめ、あめ… → [あめ=ame, あめ=ame] → S1 | „Ein Wort fliegt an ihr vorbei…" + „Ame. …Der Regen?" | – |
| 4 | P05 | signage → かさ → [かさ=lex_ja_kasa] → M | „Drinnen: trocken…" + „Kasa. Schirme. Das ist ja fast fair." | – |
| 5 | P07 | ladenbesitzer → あめ、あめ！ → [ame, ame] → S2; ladenbesitzer → これ？ → [これ=lex_ja_kore] → S2b | „Sie versteht kein Wort. Aber sie versteht alles." + „Sag irgendwas…" | speak, promptText=„Mira braucht Hilfe. Hör das Wort an und sprich es: すみません.", target=すみません, targetItemIds=[lex_ja_sumimasen], reactionAsset=assets/story/p05_reaction.jpg, reactionCaption=„Er hat sie verstanden. Ihr erstes Wort in diesem Land — und es funktioniert." |
| 6 | P17 | ladenbesitzer → これ、こわれた → [kore, kowareta] → S2; ladenbesitzer → こわれた、こわれた → [kowareta, kowareta] → S2b; protagonist → …こわれた…？ → [kowareta] → S1 | „Kowareta. Kaputt. Wie ich das erste Wort meiner Großmutter lerne: über einen kaputten Schirm." | – |
| 7 | P21 | ladenbesitzer → はい。かさ。どうぞ → [hai, kasa, douzo] → S2; protagonist → え？ → [え∅] → S1; ladenbesitzer → どうぞ、どうぞ。かさ！ → [douzo, douzo, kasa] → S2b | „Sie hat nichts bestellt…" | – |
| 8 | P22 | ladenbesitzer → はいはい → [はい=hai, はい=hai] → S2; protagonist → ありがとう… すみません… あめ… かさ… → [arigatou, sumimasen, ame, kasa] → S1 | „Es gibt genau ein Wort, das jetzt reicht…" | speak, promptText=„Sag es ihm: ありがとう.", target=ありがとう, targetItemIds=[lex_ja_arigatou], reactionAsset=assets/story/p08_reaction.jpg, reactionCaption=„Ihr zweites Wort. Es wird nicht das letzte sein." |
| 9 | P11 | signage → あめやどり → [あめ=ame, やどり∅] → M | „Das Zeichen vom Zettel…" + „Das ist kein Zufall. Oder?" | – |
| 10 | P03 | – (keine) | „Auf dem Zettel standen einmal drei Zeilen. Mira kennt jetzt: ein Zeichen und vier Wörter. Hinter dieser Tür fängt der Rest an." | – |

(∅ = Token ohne itemId: antippbar zum Vorlesen, kein Lernstoff. Deutsche Texte oben sind gekürzt zitiert — WÖRTLICH aus dem Drehbuch übernehmen.)

**Dichte-Nachweis** (so muss der Test rechnen): Bubble-Token: P1:1, P3:3, P4:1, P5:3, P6:5, P7:7, P8:6, P9:2 = **28**; + 2 speak-`target`s (すみません, ありがとう) = **30**. Pro Wort (Bubbles+targets): ame 7 · kasa 4 · kowareta 4 · douzo 3 · hai 3+2(はいはい)=… zählwerkgenau: hai kommt in P7(1)+P8(2)=3 · kore 2 · sumimasen 1+1(target)=2 · arigatou 1+1(target)=2. Alle ≥2 ✓.

`intro`/`outro` der Episode: unverändert die V1-Texte ERSETZEN durch die Drehbuch-Fassung — `intro` = Kästen-Kurzform („Eine junge Frau steigt allein aus dem Zug — es regnet. In ihrer Hand: ein Zettel, dessen Tinte verläuft.") bleibt; `outro` NEU aus Drehbuch P10-Haken: „Auf dem Zettel standen einmal drei Zeilen. Mira kennt jetzt: ein Zeichen und vier Wörter. Hinter dieser Tür fängt der Rest an."

---

### Task 1: Interaktions-Erweiterung — `promptText`, `target`, `targetItemIds`

**Files:**
- Modify: `lib/features/story/episode.dart` (class StoryInteraction)
- Modify: `lib/features/story/story_reader_screen.dart` (`_maybeShowSpeak`, `_maybeShowTrace`)
- Modify: `lib/features/story/diegetic_speak_sheet.dart`, `lib/features/story/diegetic_trace_sheet.dart` (optionaler Aufgabentext)
- Test: `test/features/story/episode_test.dart`, `test/features/story/story_reader_screen_test.dart`

**Interfaces:**
- Produces: `StoryInteraction.promptText` (String?), `.target` (String?), `.targetItemIds` (List<String>?) — fromJson-Schlüssel gleichnamig (`targetItemIds` als `List<dynamic>?` → `List<String>`), alle Default null. `DiegeticSpeakSheet`/`DiegeticTraceSheet` bekommen `final String? taskText;` (Konstruktor optional); Anzeige: `taskText ?? 'Hör zu und sprich nach:'` bzw. `taskText ?? 'Zeichne das Zeichen nach:'`. Reader-Semantik: speak/trace nutzen `interaction.target ?? <bisherige Ableitung>` als targetText, `interaction.targetItemIds ?? <bisherige Ableitung>` als itemIds (leere Liste = Erfolg feuert Reaktion, aber `onDiegetic*Success` wird mit `[]` gerufen → SRS-No-Op), `interaction.promptText` als `taskText`.

- [ ] **Step 1: Failing Tests.** `episode_test.dart` anhängen:

```dart
  test('StoryInteraction traegt promptText, target und targetItemIds', () {
    final it = StoryInteraction.fromJson({
      'type': 'trace', 'diegetic': true,
      'promptText': 'Rette das Zeichen.',
      'target': 'め',
      'targetItemIds': <dynamic>[],
    });
    expect(it.promptText, 'Rette das Zeichen.');
    expect(it.target, 'め');
    expect(it.targetItemIds, isEmpty);
    final without = StoryInteraction.fromJson({'type': 'speak'});
    expect(without.promptText, isNull);
    expect(without.target, isNull);
    expect(without.targetItemIds, isNull);
  });
```

`story_reader_screen_test.dart` anhängen (Helfer der Datei nutzen; frisches Öffnen braucht den Titelkarten-Tap):

```dart
  testWidgets('speak-Interaktion mit target/promptText/targetItemIds nutzt '
      'genau diese statt der Bubble-Ableitung', (tester) async {
    final json = _episodeWithDiegeticSpeakOnSecondPanelJson();
    final panel = ((json['pages'] as List).first
        as Map<String, dynamic>)['panels'][1] as Map<String, dynamic>;
    (panel['interactions'] as List)[0] = {
      'type': 'speak', 'diegetic': true,
      'promptText': 'Mira braucht Hilfe.',
      'target': 'すみません',
      'targetItemIds': ['lex_ja_sumimasen'],
    };
    final received = <String>[];
    await tester.pumpWidget(MaterialApp(
      home: StoryReaderScreen(
        episode: Episode.fromJson(json),
        progressStore: await _freshStore(),
        speak: _noopSpeak,
        dictionaryEntries: const [],
        knownIds: const {},
        speakEvaluator: _FakeSpeakEvaluator(0.9),
        onDiegeticSpeakSuccess: (ids) async => received.addAll(ids),
      ),
    ));
    await tester.pumpAndSettle();
    await tester.tap(find.byKey(const ValueKey('story-title-card')));
    await tester.pumpAndSettle();
    await tester.tap(find.byKey(const ValueKey('story-reader-panel')));
    await tester.pumpAndSettle();
    expect(find.text('Mira braucht Hilfe.'), findsOneWidget);
    expect(find.text('すみません'), findsWidgets); // das Ziel, nicht der Bubble-Text
    await tester.tap(find.byKey(const ValueKey('diegetic-speak-mic')));
    await tester.pump(kDiegeticSuccessAutoClose);
    await tester.pumpAndSettle();
    expect(received, ['lex_ja_sumimasen']);
  });

  testWidgets('trace mit target und leeren targetItemIds: Aufgabe erscheint, '
      'Erfolg reagiert, aber bucht nichts', (tester) async {
    final json = _episodeWithDiegeticTraceOnSecondPanelJson();
    final panel = ((json['pages'] as List).first
        as Map<String, dynamic>)['panels'][1] as Map<String, dynamic>;
    (panel['interactions'] as List)[0] = {
      'type': 'trace', 'diegetic': true,
      'promptText': 'Rette das Zeichen: め.',
      'target': 'め',
      'targetItemIds': <dynamic>[],
      'reactionCaption': 'Jetzt gehört es ihr.',
    };
    final received = <String>[];
    await tester.pumpWidget(MaterialApp(
      home: StoryReaderScreen(
        episode: Episode.fromJson(json),
        progressStore: await _freshStore(),
        speak: _noopSpeak,
        dictionaryEntries: const [],
        knownIds: const {},
        traceEvaluator: _FakeTraceEvaluator(true),
        onDiegeticTraceSuccess: (ids) async => received.addAll(ids),
      ),
    ));
    await tester.pumpAndSettle();
    await tester.tap(find.byKey(const ValueKey('story-title-card')));
    await tester.pumpAndSettle();
    await tester.tap(find.byKey(const ValueKey('story-reader-panel')));
    await tester.pumpAndSettle();
    expect(find.text('Rette das Zeichen: め.'), findsOneWidget);
    await tester.drag(find.byKey(const ValueKey('diegetic-trace-canvas')),
        const Offset(30, 30));
    await tester.tap(find.byKey(const ValueKey('diegetic-trace-done')));
    await tester.pump(kDiegeticSuccessAutoClose);
    await tester.pumpAndSettle();
    expect(received, isEmpty);
    expect(find.byKey(const ValueKey('story-reaction-caption')), findsOneWidget);
  });
```

(Falls `_episodeWithDiegeticTraceOnSecondPanelJson()` noch nicht als Map-Helfer existiert, den bestehenden `_episodeWithDiegeticTraceOnSecondPanel()`-Helfer wie zuvor bei den anderen in eine `...Json()`-Map heben. Falls der Fake-Trace-Erfolg mehr Striche braucht (`_FakeTraceEvaluator` prüfen), die drag-Zeile entsprechend wiederholen.)

- [ ] **Step 2: Fail zeigen.** Run: `flutter test test/features/story/episode_test.dart test/features/story/story_reader_screen_test.dart` · Expected: neue Fälle FAIL (`promptText` unbekannt).
- [ ] **Step 3: Implementieren.**

`episode.dart`, class StoryInteraction — Felder nach `reactionCaption`:

```dart
  /// Deutsche Aufgabenzeile aus dem Drehbuch (ersetzt den Standardtext
  /// des Sheets). Null = generischer Text.
  final String? promptText;

  /// Explizites Sprech-/Zeichenziel. Null = Ableitung aus den
  /// Panel-Bubbles (bisheriges Verhalten).
  final String? target;

  /// Explizite SRS-Buchung bei Erfolg. Null = Ableitung aus den
  /// Bubble-Tokens; leere Liste = Erfolg reagiert nur erzählerisch
  /// (z. B. Glyph-Momente wie め, die kein Lexem sind).
  final List<String>? targetItemIds;
```

Konstruktor + fromJson:

```dart
      promptText: j['promptText'] as String?,
      target: j['target'] as String?,
      targetItemIds: (j['targetItemIds'] as List?)?.cast<String>(),
```

`story_reader_screen.dart` — in `_maybeShowSpeak`: die Interaktion, die den Match ergab, in eine lokale Variable heben (`final interaction = ...` aus der bestehenden Suche); dann:

```dart
    final targetText = interaction.target ??
        panel.bubbles.map((b) => b.text).join(' ');
    final itemIds = interaction.targetItemIds ?? derivedItemIds;
```

(`derivedItemIds` = die BESTEHENDE itemIds-Sammlung der Methode, unverändert in eine lokale Variable dieses Namens gehoben — reine Umbenennung, keine Logikänderung.)

und beim Sheet-Bau `taskText: interaction.promptText` ergänzen. `_maybeShowTrace` analog (target ersetzt die Token-Join-Ableitung, targetItemIds die itemId-Sammlung; die inerte-Notiz-Ausschlusslogik bleibt für den Ableitungsfall bestehen).

Sheets: Feld `final String? taskText;` + Konstruktor-Param; im build die konstante Aufgaben-Zeile ersetzen durch `Text(taskText ?? 'Hör zu und sprich nach:')` bzw. `Text(taskText ?? 'Zeichne das Zeichen nach:')`.

- [ ] **Step 4: Pass zeigen.** Run: `flutter test test/features/story/ 2>&1 | tail -2` · Expected: alles grün.
- [ ] **Step 5: Commit.** `git add lib/features/story/ test/features/story/ && git commit -m "feat(story): Interaktionen tragen Drehbuch-Aufgaben, explizite Ziele und Buchungslisten (Folge01-V2)"`

---

### Task 2: Fixture V2 — die Folge nach Drehbuch, mit Dichte-Test

**Files:**
- Modify: `lib/features/story/episodes/folge_01_regen.dart` (`pilot01RegenJson` KOMPLETT ersetzen nach Bauplan; `folge01DictionaryEntries` unverändert)
- Create: `test/features/story/folge_01_dichte_test.dart`
- Modify (nachziehen): `test/features/story/story_route_test.dart`, `test/features/story/story_reader_srs_handoff_test.dart`, ggf. weitere `pilot01RegenJson`-Nutzer

**Interfaces:**
- Consumes: Task-1-Felder; Bauplan-Tabelle + Drehbuch (Wortlaut!).
- Produces: `pilot01RegenJson` mit 10 Panels (Seiten-Aufteilung: Seite 0 = P1–P4, Seite 1 = P5–P8, Seite 2 = P9–P10; Panel-`index` 0–9 fortlaufend), gleiche 8 Budget-Items + Glyphen あ/め/か, `intro`/`outro` wie im Bauplan-Kopf, hitAreas aus den Slots, `notes` je Panel = die Bild-Beschreibung aus dem Drehbuch (für die spätere ckpt-6-Renderarbeit).

- [ ] **Step 1: Failing Dichte-Test schreiben** — `test/features/story/folge_01_dichte_test.dart` (komplett):

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:nihongo_app/features/story/episode.dart';
import 'package:nihongo_app/features/story/episodes/folge_01_regen.dart';

void main() {
  test('Folge 01 erfuellt das Dichte-Soll des Formats', () {
    final episode = loadFolge01();
    final tokenCounts = <String, int>{};
    var bubbleTokens = 0;
    for (final panel in episode.allPanels) {
      for (final bubble in panel.bubbles) {
        for (final token in bubble.tokens) {
          bubbleTokens++;
          final id = token.itemId;
          if (id != null) tokenCounts[id] = (tokenCounts[id] ?? 0) + 1;
        }
      }
    }
    var targetHits = 0;
    for (final it in episode.allPanels.expand((p) => p.interactions)) {
      for (final id in it.targetItemIds ?? const <String>[]) {
        targetHits++;
        tokenCounts[id] = (tokenCounts[id] ?? 0) + 1;
      }
    }
    expect(bubbleTokens, greaterThanOrEqualTo(28),
        reason: 'zu wenig Sprache in den Blasen');
    expect(bubbleTokens + targetHits, greaterThanOrEqualTo(30));
    for (final ref in episode.budget.items) {
      expect(tokenCounts[ref.id] ?? 0, greaterThanOrEqualTo(2),
          reason: '${ref.id} kommt zu selten vor (Wiederholung ist das Lernen)');
    }
    expect(episode.allPanels.length, 10);
    expect(episode.intro, isNotNull);
    expect(episode.outro, isNotNull);
  });
}
```

- [ ] **Step 2: Fail zeigen.** Run: `flutter test test/features/story/folge_01_dichte_test.dart` · Expected: FAIL (V1 hat 24 Panels / zu wenig Tokens).
- [ ] **Step 3: Fixture bauen.** `pilot01RegenJson` vollständig nach Bauplan-Tabelle ersetzen: pro Panel `index/asset(assets/story/p01..p10.jpg)/bubbles(text+tokens+hitArea aus Slot)/thoughts(WÖRTLICH aus Drehbuch: erst Kästen, dann Gedanke — Gedanken in „…" setzen)/interactions(mit promptText/target/targetItemIds/reactionAsset/reactionCaption aus Tabelle)/notes(Drehbuch-Bildbeschreibung)`. Budget/Glyphen/id/title/locale/era unverändert; `intro`/`outro` aus dem Bauplan-Kopf.
- [ ] **Step 4: Bestand nachziehen + Pass zeigen.** Durchles-Schleifen: 23 Taps → **9** (10 Panels) in `story_route_test.dart` und `story_reader_srs_handoff_test.dart` (Kommentar dort anpassen); `grep -rn "pilot01RegenJson\|23" test/features/story/ test/features/mining_slice/` als Checkliste — Erwartungen ehrlich anpassen (z. B. Panel-Zahl-Assertions), nichts löschen. Run: `flutter test test/features/story/ test/features/mining_slice/ 2>&1 | tail -2` · Expected: grün BIS AUF `folge_01_panel_assets_test` (Assets p01–p10 existieren noch als V1-Bilder — Test bleibt grün, da Pfade existieren; falls rot wegen fehlender p0X_reaction-Namen → das liefert Task 3, dann diesen Lauf dort wiederholen und hier nur die Nicht-Asset-Tests grün melden).
- [ ] **Step 5: Commit.** `git add lib/features/story/episodes/folge_01_regen.dart test/ && git commit -m "feat(story): Folge 01 = Drehbuch V2 — 10 dichte Panels, Mira, Zettel-Trace, Dichte-Test (Folge01-V2)"`

---

### Task 3: Lettering V2 + Assets

**Files:**
- Modify: `tool/letter_folge01.py` (Mapping + Bubbles + Reaktionen auf V2)
- Assets: neue `assets/story/p01..p10.jpg`, `p02_reaction.jpg`, `p05_reaction.jpg`, `p08_reaction.jpg`; ALTE `p11.jpg…p24.jpg`, `p07_reaction.jpg`, `p22_reaction.jpg`, `p24_reaction.jpg` per `git rm` entfernen
- Test: `flutter test test/features/story/folge_01_panel_assets_test.dart` (bestehend, prüft dynamisch)

**Interfaces:**
- Consumes: Bauplan (Quell-Render-Spalte, Bubble-Texte+Slots), Quelle `/home/uli/.claude/jobs/df1342e0/tmp/final/P##.png`.
- Produces: die 13 gebündelten V2-Bilder.

- [ ] **Step 1: Skript umbauen.** In `tool/letter_folge01.py`: neue Konstante `MAPPING = {1: 1, 2: 2, 3: 4, 4: 5, 5: 7, 6: 17, 7: 21, 8: 22, 9: 11, 10: 3}` (neu → Quell-P##); `BUBBLES` auf die V2-Tabelle umschreiben (Panel-Nr. NEU, Texte+Slots aus dem Bauplan; P1/P4/P9 = signage-Texte; P2/P10 ohne Lettering); `REACTIONS = [2, 5, 8]`; `main()` iteriert `for n in range(1, 11)` und lädt `load(MAPPING[n])`, speichert `p{n:02d}.jpg` (+`_reaction` für REACTIONS). Docstring aktualisieren (V2).
- [ ] **Step 2: Ausführen + alte Assets entfernen.** Run: `python3 tool/letter_folge01.py` → `OK …`; dann `git rm -q assets/story/p1[1-9].jpg assets/story/p2[0-4].jpg assets/story/p07_reaction.jpg assets/story/p22_reaction.jpg assets/story/p24_reaction.jpg` (exakte Liste vorher mit `ls assets/story/` prüfen — es dürfen GENAU p01–p10 + die 3 neuen _reaction übrig bleiben).
- [ ] **Step 3: Pass zeigen.** Run: `flutter test test/features/story/folge_01_panel_assets_test.dart test/features/story/folge_01_regen_test.dart test/features/story/folge_01_dichte_test.dart` · Expected: PASS.
- [ ] **Step 4: Commit.** `git add -A assets/story tool/letter_folge01.py && git commit -m "feat(story): Prototyp-Panels V2 — 10 Bilder nach Drehbuch-Mapping, 3 Reaktionen (Folge01-V2)"`

---

### Task 4: Full-Suite + Analyzer

- [ ] **Step 1:** Run: `flutter analyze 2>&1 | tail -2` — keine neuen Meldungen in berührten Dateien.
- [ ] **Step 2:** Run: `flutter test 2>&1 | grep -E "^[0-9:]+ \+[0-9]+" | tail -1` — Fehlschläge AUSSCHLIESSLICH die 8 vorbestehenden `test/mining_packs/ja/`. Jeder andere wird verortet und gefixt.
- [ ] **Step 3 (nur bei Fixes):** `git add -A && git commit -m "test(folge01-v2): Full-Suite-Regresse behoben"`

---

## Anschlussstellen (NICHT in diesem Plan)

- Deploy aufs S23 + **Ulis Erlebnis-Urteil** („trägt die Geschichte? lernt man was?") — die Abnahme.
- Finale ckpt-6-Renders für die 10 Panels (Bild-Beschreibungen stehen in den `notes`).
- Lernmechanik-Spec PR #43 nachziehen (800er-Serien-Curriculum).
