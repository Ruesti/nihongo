// The pilot episode content now lives in lib/ so the app can load it (W3);
// this fixture path is kept as a re-export so test importers are unchanged.
export 'package:nihongo_app/packs/ja/pilot_01_regen.dart';
